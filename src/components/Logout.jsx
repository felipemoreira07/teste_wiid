import { Box, Typography } from "@mui/material";

const Logout = () => {
    return (
        <Box sx={{
            display: "flex",
            alignItems: "center"
        }}>
            <Typography sx={{
                color: "blue"
            }}>
                Felipe
            </Typography>
        </Box>
    )
}

export default Logout;